# 진행 상황 & 다음 세션 인계 (STATUS)

> **한 줄 요약**: 지팡이 10종 + 인챈트 3종. **코드·데이터 완성, 런타임·인게임 검증 완료.**
> 텍스처·밸런스·로스터는 현행대로 간다고 판단했다. 남은 건 **배포 준비**(GitHub 리모트, CurseForge 프로젝트 ID)다.

시작: 2026-09-21. SY Alchemy 0.2.0의 마법 축을 떼어 내 새로 설계한 모드
(이유는 `C:\Projects\syalchemy\docs\재설계-제안.md`). 설계는 [기획.md](기획.md).

---

## 1. 빌드 & 실행

```powershell
.\gradlew.bat compileJava   # 컴파일
.\gradlew.bat runData       # 모델·en_us·레시피·태그 생성 → src/generated/resources
.\gradlew.bat runClient     # 개발용 클라이언트
.\gradlew.bat runServer     # 데디케이티드 서버 (run/eula.txt 필요, 이미 있음)
.\gradlew.bat build         # build/libs/symagic-<ver>.jar
powershell -ExecutionPolicy Bypass -File tools\gen_textures.ps1   # 플레이스홀더 텍스처
python tools/gen_logo.py                                          # 로고 512x512 (CF + 모드 목록)
```

배포 절차는 [PUBLISHING.md](../PUBLISHING.md), CurseForge 폼에 넣을 내용은
[docs/curseforge/등록정보.md](curseforge/등록정보.md).

**Gradle 데몬이 꺼져 있다.** `runServer`/`runClient`는 계속 떠 있으니 로그를 읽고 따로 죽여야 한다.

```powershell
Get-CimInstance Win32_Process | Where-Object { $_.Name -eq 'java.exe' -and $_.CommandLine -like '*Projects\symagic*' } | Stop-Process -Force
```

---

## 2. 검증된 것 (2026-09-21)

| 항목 | 결과 |
|---|---|
| `compileJava` | BUILD SUCCESSFUL |
| `runData` | 4개 프로바이더 정상, 42개 파일 생성 |
| `runServer` | `SY Magic loaded.` · `Loaded 1595 recipes`(바닐라 1585 + 지팡이 10) · `Done` · 모드 오류 0 |
| `build` | `symagic-0.1.0.jar` |
| `runClient` (11분 세션) | `SY Magic loaded.` · symagic 관련 예외 0 · 누락 모델/텍스처 경고 0. ERROR는 전부 oshi(윈도우 하드웨어 정보)의 개발환경 잡음 |
| **마법부여대 노출** | **인게임 확인함** — 마력 · 신속 시전 · 마력 확산 셋 다 뜬다 |
| **점멸 착지** | **인게임 확인함** — 벽에 끼지 않는다 (0.6블록 후퇴가 실제로 동작) |
| **돌풍 밀어내기** | **인게임 확인함** — 클라이언트에 보인다 (`hurtMarked` 필요했던 이유) |
| 텍스처 | 현행 플레이스홀더로 문제 없다고 판단 (2026-09-21) |
| 밸런스 · 로스터 | 현행 10종 · 쿨다운 · 내구로 문제 없다고 판단 (2026-09-21) |
| `#minecraft:non_treasure` 태그 병합 | **양성 확인함** — 일부러 없는 인챈트 id를 넣었더니 바닐라가 `Unbound values in registry minecraft:enchantment`로 거부했다. 즉 우리 태그 파일이 실제로 바닐라 인챈트 레지스트리에 반영된다 |

**인게임 확인 완료.** 위험했던 두 지점(점멸 착지 · 돌풍 동기화)까지 직접 봤다. 남은 것은 배포 준비뿐이다.

---

## 3. 코드 구조 — 표 하나가 모드 전체다

```
src/main/java/com/syang/symagic/
├── SyMagic.java                    @Mod — ModItems.register + ModCreativeTabs.register 가 전부
├── registry/
│   ├── ModItems.java               ModStaff enum을 돌며 지팡이 10개 등록
│   ├── ModCreativeTabs.java        탭 1개
│   └── ModEnchantments.java        인챈트 3종 키 + 레벨→계수(spellPower/cooldown/reach)
├── world/item/
│   ├── ModStaff.java               ★ 재료·원소·쿨·내구·인챈트성·마법이 한 줄씩
│   ├── StaffItem.java              use() = 시전 + 쿨다운 + 내구 1 + 소리
│   └── Element.java                툴팁 색/글리프 (플레이 규칙 없음)
├── world/spell/SpellEffects.java   마법 10종 구현
└── datagen/                        ModDataGenerators + 모델·en_us·레시피·아이템태그
src/main/resources/
├── data/symagic/enchantment/{spell_power,alacrity,arcane_reach}.json
└── data/minecraft/tags/enchantment/non_treasure.json   ← 바닐라 획득 경로에 올리는 한 줄
```

### 지팡이 추가하는 법

1. `ModStaff`에 한 줄 (id · 표시명 · 재료 · 원소 · 쿨 · 내구 · 인챈트성 · 메서드 참조)
2. `SpellEffects`에 `(ServerLevel, Player, ItemStack, float) -> void` 메서드 하나
3. `ModEnglishLangProvider.SPELLS`에 영문 설명 한 줄 + `assets/symagic/lang/ko_kr.json`에 이름·설명
4. `tools/gen_textures.ps1`의 `$staffColors`에 색 하나 → 스크립트 재실행
5. `.\gradlew.bat runData`

레시피·모델·태그·advancement는 전부 자동 생성된다.

---

## 4. 핵심 설계 결정 (다음 세션 필독)

- **지팡이 하나에 마법 하나.** 그래서 데이터 컴포넌트가 없다. 스택에 아무것도 저장하지 않으므로
  네트워크 동기화·휠 전환·모루 부여·숫돌 초기화가 전부 필요 없다. 전작에서 코드의 절반이 여기서 나왔다.
- **인챈트는 만들 수 없다.** `#minecraft:non_treasure`에 올려 두면 바닐라가 알아서
  `in_enchanting_table` · `tradeable` · `on_random_loot`에 접어 넣는다. 우리가 제공하는 획득 경로는 0개다.
  이게 "바닐라의 일을 빼앗지 않는다"는 원칙의 구현 전부다.
- **지팡이는 무기 태그에 넣지 않는다.** `#minecraft:enchantable/durability`와 `/vanishing`,
  그리고 `#symagic:enchantable/staff`뿐. 무기·채굴 태그에 넣으면 날카로움이 붙어 버린다.
- **인챈트성 0이면 마법부여대에 안 뜬다.** `Item.Properties#enchantable(n)`을 반드시 준다(자수정 22, 나머지 12~18).
- **화살 데미지 함정**: 바닐라 `AbstractArrow`는 데미지를 `ceil(비행속도 × baseDamage)`로 계산한다.
  서리 화살은 `baseDamage = 5×power / velocity`로 속도를 나눠 상쇄한다 —
  안 그러면 마력 확산이 사거리뿐 아니라 **데미지까지** 배로 키운다. (전작에서 실제로 겪은 버그다.)
- **점멸 착지**: 시야 광선이 맞은 지점에서 `0.6` 블록 되돌아온 자리에 내린다. 안 그러면 블록 안에 낀다.
  `resetFallDistance()`를 같이 불러 낙하 피해를 지운다.
- **돌풍의 `hurtMarked = true`**: 안 붙이면 서버가 속도 변화를 클라이언트에 안 보낸다.

---

## 5. 이 환경의 함정

- **줄 끝을 섞지 말 것.** 저장소는 LF로 보관하지만 `core.autocrlf` 때문에 새로 체크아웃하면 작업본이
  CRLF가 된다. 편집 전에 `file <경로>`로 실제 상태를 보고 맞춘다 — 매칭은 `\R`로 하되 치환문에는
  `\R`을 쓸 수 없으니 그 파일이 실제로 쓰는 줄 끝을 직접 적는다.
- **PowerShell/Gradle 로그가 UTF-16일 수 있다.** grep 전에 `tr -d '\000'`.
- **`python`은 된다.** 3.13.14 + Pillow 12.3 설치돼 있다(Store 배포판이지만 스텁이 아니라 실물이다).
  형제 프로젝트가 그렇듯 이미지 생성은 python/Pillow로 쓴다 — `tools/gen_logo.py`.
  (텍스처 생성 `tools/gen_textures.ps1`만 PowerShell `System.Drawing`으로 남아 있다.)
- **26.2 API 참조**: `build/moddev/artifacts/minecraft-patched-26.2.0.88-sources.jar`를 풀어서 grep.
  바닐라 데이터(태그 JSON 등)는 `minecraft-patched-26.2.0.88.jar` 안에 있다.

---

## 6. 다음 작업 후보

배포를 막고 있는 것부터:

1. ~~GitHub 저장소 생성 + push~~ — **완료.** https://github.com/syang0531/symagic (Public).
   `build.yml` 통과 확인. 첫 실행은 `gradlew`가 100644로 커밋돼 있어 `Permission denied`로 죽었다 —
   Windows Git은 실행 비트를 안 가져오므로 `git update-index --chmod=+x`로 인덱스에 직접 넣어야 한다.
2. ~~CurseForge 프로젝트 등록 + `curseforge_project_id` + `CURSEFORGE_TOKEN` 시크릿~~ — **완료.**
   프로젝트 ID `1705439`, slug `symagic`(그래서 `mod_homepage`는 그대로). 시크릿은 2026-09-21 등록됨.
3. **`v0.1.0` 태그 푸시**로 첫 릴리스.

그다음:

4. **텍스처 아트** — 플레이스홀더로 릴리스는 가능하다고 판단했지만, 올릴 여지는 남아 있다.
5. **원소 상성 · 시전 경험치** — 기획 §6의 열어 둔 것. 플레이가 쌓인 뒤에 결정한다.
